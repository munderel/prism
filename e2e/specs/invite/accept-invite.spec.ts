import { test } from '../../fixtures/test';

test.describe('@deep accept invite', () => {
  test.fixme('open invite URL while logged in auto-accepts or shows switch-account UI', async () => {
    // Requires generating an invitation via /api/invitations and a second user identity.
    // Without a second test account, this is `test.fixme` until that's set up.
  });
});
