import { test, expect } from '../../fixtures/test';

test.describe('@deep toasts and empty states', () => {
  test.fixme('toast appears on task create/save', async () => {});
  test.fixme('toast appears on delete', async () => {});
  test.fixme('empty state copy when no tasks for the day', async () => {});
  test.fixme('empty state copy when no ideas', async () => {});
  test.fixme('empty state copy when no streaks', async () => {});
});
